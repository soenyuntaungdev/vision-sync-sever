__pycache__/
*.pyc

# Runtime artifacts & secrets — repo ထဲ မထည့်ရ
active_model.json
reports_log.json
runs/
models/
uploads/

# Model weights — repo size limit ထက် ကျော်လွန်ပါသည် (GitHub limit: 100MB)
*.pt
weights/

# COCO replay download cache (ပြန် download လုပ်လို့ရသည် — repo ထဲ မထည့်ရ)
.cache/

# Dataset ပုံ/label များ (master/data.yaml ကိုတော့ ထားမည်)
dataset/*/images/
dataset/*/labels/
dataset/*/.coco_replay.json
*.cache
